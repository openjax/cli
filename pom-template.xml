<project
	xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.safris</groupId>
		<artifactId>commons</artifactId>
		<version>${dev:org.safris.commons}</version>
	</parent>
	<groupId>org.safris.commons</groupId>
	<artifactId>cli</artifactId>
	<version>${dev:org.safris.commons.cli}</version>
	<name>${groupId}.${artifactId}</name>
	<description>
		Command-line interface.
	</description>
	<profiles>
		<profile>
			<activation>
				<property>
					<name>!task-segment:site</name>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.safris.maven.plugin</groupId>
						<artifactId>cobertura</artifactId>
						<version>${dev:org.safris.maven.plugin.cobertura}</version>
						<executions>
							<execution>
								<goals>
									<goal>instrument</goal>
									<goal>report</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
	<build>
		<plugins>
			<plugin>
				<groupId>org.safris.maven.plugin.xml</groupId>
				<artifactId>binding</artifactId>
				<version>${dev:org.safris.maven.plugin.xml.binding}</version>
				<executions>
					<execution>
						<phase>generate-sources</phase>
						<goals>
							<goal>generate</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<manifest xmlns="http://xml.safris.org/toolkit/binding/manifest.xsd">
						<destdir explodeJars="true">${project.build.directory}/generated-sources/xmlbinding</destdir>
						<schemas>
							<schema>${basedir}/src/main/resources/xsd/cli.xsd</schema>
						</schemas>
					</manifest>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>org.safris.xml.toolkit</groupId>
			<artifactId>binding</artifactId>
			<version>${dev:org.safris.xml.toolkit.binding}</version>
		</dependency>
		<dependency>
			<groupId>org.safris.commons</groupId>
			<artifactId>el</artifactId>
			<version>${dev:org.safris.commons.el}</version>
		</dependency>
		<dependency>
			<groupId>commons-cli</groupId>
			<artifactId>commons-cli</artifactId>
		</dependency>
	</dependencies>
</project>
